/**
 * Directus interface for board position items
 */
export interface BoardPositionItem {
  portrait: string;
  name: string;
  position_description: string;
  position: string;
  order: number;
  email: string;
}
